[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "treeco"
version = "0.1.0"
description = "Tree size and carbon estimation from CommuniMap citizen-science imagery"
readme = "README.md"
requires-python = ">=3.10"
authors = [
    { name = "Federico Stachurski" }
]
dependencies = [
    "numpy",
    "pandas",
    "pillow",
    "matplotlib",
    "scikit-learn",
    "torch",
    "torchvision",
    "tqdm",
    "rasterio",
    "pyproj"
]

[project.scripts]
treeco-build-manifest = "treeco.data.build_manifest:main"
treeco-estimate-carbon = "treeco.carbon.estimate_carbon:main"
treeco-predict = "treeco.inference.predict:main"

treeco-download-models = "treeco.image_models.download_cli:main"
treeco-build-dataset = "treeco.pipeline.build_dataset_cli:main"
treeco-train-height = "treeco.training.training_height_cli:main"
treeco-train-width = "treeco.training.training_width_cli:main"
treeco-plot-training = "treeco.training.plot_training_run_cli:main"
treeco-infer-heights = "treeco.inference.infer_heights_cli:main"
treeco-infer-widths = "treeco.inference.infer_widths_cli:main"
treeco-combine-height-width = "treeco.utils.combine_height_width_cli:main"
treeco-make-map = "treeco.mapping.make_map_cli:main"
[tool.setuptools.packages.find]
where = ["src"]
include = ["treeco*"]
